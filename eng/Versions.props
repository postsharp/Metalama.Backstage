<Project>

    <!-- Version of PostSharp.Backstage -->
    <Import Project="MainVersion.props" />
    
    <PropertyGroup>
        <PostSharpBackstageSettingsVersion>$(MainVersion)$(PackageVersionSuffix)</PostSharpBackstageSettingsVersion>
        <PostSharpBackstageSettingsAssemblyVersion>$(MainVersion)</PostSharpBackstageSettingsAssemblyVersion>
    </PropertyGroup>

    <!-- Versions of dependencies -->
    <PropertyGroup>
    </PropertyGroup>

    <!-- Overrides by local settings -->
    <Import Project="../artifacts/private/PostSharp.Backstage.Settings.version.props" Condition="Exists('../artifacts/private/PostSharp.Backstage.Settings.version.props')" />
    <Import Project="Dependencies.props" Condition="Exists('Dependencies.props')" />

    <!-- Other properties dependent on the versions set above. -->
    <PropertyGroup>
        <AssemblyVersion>$(PostSharpBackstageSettingsAssemblyVersion)</AssemblyVersion>
        <Version>$(PostSharpBackstageSettingsVersion)</Version>
    </PropertyGroup>

</Project>
